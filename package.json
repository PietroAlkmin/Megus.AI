{
  "name": "megus-ai",
  "version": "0.0.1",
  "private": true,
  "description": "Megus AI — atendente virtual de WhatsApp (Kaua). Agnóstico de provedor de mensageria (WPP→Meta) e de backend fiscal (mock→ERP).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.67.0",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "//deps-pendentes": "Mensageria (wppconnect/baileys/whatsapp-cloud) entra após a pesquisa wr80xu2as. Persistência (Postgres/Prisma) e Redis entram após confirmação de infra."
}
